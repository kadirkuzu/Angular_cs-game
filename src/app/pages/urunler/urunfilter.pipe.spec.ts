import { UrunfilterPipe } from './urunfilter.pipe';

describe('UrunfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UrunfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
